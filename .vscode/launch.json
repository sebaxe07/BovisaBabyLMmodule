{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Run from Project Root",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "justMyCode": true
        }
    ]
}